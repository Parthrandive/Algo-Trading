# Core Data & processing
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0  # For Parquet file handling (Bronze/Silver/Gold storage)

# Market Data (Indian Context)
nsepython>=0.1.0  # Unofficial NSE API wrapper (as per plan)
yfinance>=0.2.36  # Secondary/Validation source
requests>=2.31.0

# Validation & Config
pydantic>=2.6.0  # Data contracts & Schema validation
python-dotenv>=1.0.1  # Secrets management
pyyaml>=6.0.1

# Utilities & Resilience
tenacity>=8.2.3  # Retry logic for network calls
structlog>=24.1.0  # Structured logging for defining observability
pytz>=2024.1     # Timezone handling (UTC+IST)
pdfplumber>=0.11.0  # PDF transcript extraction for textual adapters

# Testing & Quality
pytest>=8.0.0
pytest-cov>=4.1.0
ruff>=0.2.1      # Linting (fast)
black>=24.1.1    # Formatting
ntplib>=0.4.0    # NTP client for clock sync checks

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
schedule==1.2.2
# Phase 2 ML Dependencies
statsmodels>=0.14.0
torch>=2.2.0
arch>=7.0.0
scikit-learn>=1.4.0
scipy>=1.12.0
forex-python>=1.6  # Fetching bulk historical ECB fx rates
